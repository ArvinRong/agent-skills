interface:
  display_name: "Project Skill Finder"
  short_description: "Find and route into project-local skill docs"
  default_prompt: "Use $project-skill-finder to discover project-local skill docs and load only the most relevant ones for the current repository task."
policy:
  allow_implicit_invocation: true
